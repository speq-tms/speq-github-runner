name: speq nightly regression

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

jobs:
  speq-nightly:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Materialize SPEQ CI environment
        env:
          SPEQ_CI_BASE_URL: ${{ secrets.SPEQ_CI_BASE_URL }}
          SPEQ_CI_SOURCE_HEADER: ${{ secrets.SPEQ_CI_SOURCE_HEADER }}
        run: |
          python3 - <<'PY'
          import json
          import os
          from pathlib import Path

          base_url = os.environ.get("SPEQ_CI_BASE_URL") or "https://jsonplaceholder.typicode.com"
          source = os.environ.get("SPEQ_CI_SOURCE_HEADER") or "speq-nightly"

          path = Path(".speq/environments/ci.yaml")
          path.parent.mkdir(parents=True, exist_ok=True)
          path.write_text(
              "\n".join(
                  [
                      "name: ci",
                      f"baseUrl: {json.dumps(base_url)}",
                      "headers:",
                      f"  x-source: {json.dumps(source)}",
                      "",
                  ]
              ),
              encoding="utf-8",
          )
          PY

      - name: Run full nightly suite
        uses: speq-tms/speq-github-runner@v1
        with:
          mode: run
          speq-root: .speq
          env: ci
          suite: suites
          artifacts-prefix: speq-nightly
